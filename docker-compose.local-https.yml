services:
  portal:
    ports:
      - "${PORTAL_HTTP_PORT:-3000}:80"
      - "${PORTAL_HTTPS_PORT:-3443}:443"
    volumes:
      - ./nginx/default.local-ssl.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
